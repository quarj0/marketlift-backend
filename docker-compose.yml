services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-marketlift}
      POSTGRES_USER: ${POSTGRES_USER:-marketlift}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-marketlift}
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - marketlift_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-marketlift} -d ${POSTGRES_DB:-marketlift}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - marketlift_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  marketlift_postgres_data:
  marketlift_redis_data:
